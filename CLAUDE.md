# DotShot（crypto-peggle）ガイドライン

> このファイルは **crypto-peggle（DotShot）専用**。親 `../CLAUDE.md`（Baseapp 全体共通）とは別に、
> このリポジトリを開いたスレッドだけが従う。共通ルール（OG/Satori 実装ルール等）は親を参照すること。

---

## 1. 設計思想 — 宇宙感の段階的拡張（North Star）

> プロダクトコンセプト全体は **[docs/CONCEPT.md](docs/CONCEPT.md)** を参照
> （1ラン=1潜行・深度競争・Base=潜行の痕跡・隠された発見の4本柱＋アンチゴール）。

DotShot は「**レベルが進むほど宇宙感が増していく**」方向で育てる。手段は
**レベルが進むほどに「宇宙の未知のもの」を新ギミックとして1つずつ追加していく**こと。

- 浅いレベル＝身近な障害物。**深いレベルほど、未知で異様な宇宙現象**が増える（不気味・強力・予測困難になっていく）。
- **背景（紙）は全レベル通してクリーム地 `#ede9df` で不変**（2026-07-13 再改定: 深度で紙が暗くなる
  深度パレットは撤去した。skybox 禁止も不変）。背景側の深度表現は**塵の挙動**で行う:
  既存の枯れ系（減速・希薄化・縁寄せ・中央ホロー・lv81+グリッド化）＋**マリンスノー**
  （`depthSnowT`: lv30→90 で塵のドリフトが下向きに揃い、深部では静かに降り積もるように落ちる）。
  後半は加えて **Paper Mood**（lv40+・レベルごと1主ムード＋稀レア）で紙の癖を変える
  （[docs/PAPER_MOODS.md](docs/PAPER_MOODS.md)。紙色は不変・物理非接触）。
  canvas 内で紙色を参照するときは描画ループ冒頭の `paperColor`（定数）を使い、ハードコードしない。
  宇宙感の主体は **ハザード（と既存の霧 lv17+）** であることも不変。
- 現デザインを**大きく崩さない**ことが最優先。新要素も既存の見た目の文法に従わせる（§2）。

---

## 2. 崩してはいけないデザイン契約（Visual Contract）

> ハザードの視認性基準（Tier原則）・禁止色帯・色ファミリーマップ・全種判定表は
> **[docs/GIMMICK_DESIGN_GUIDE.md](docs/GIMMICK_DESIGN_GUIDE.md)** を参照。

新しく描くものは、必ず次のルールに従う。

### パレット
| 役割 | 色 |
|---|---|
| 背景（紙） | `#ede9df`（全レベル不変）。canvas 内では描画ループ冒頭の `paperColor` 定数を参照する |
| インク（通常ピース・UI文字） | `#0f0f0d`（全深度で不変） |
| ミュート（副次UI文字） | `#7a7670` |
| ゴールド（アクセント） | `#c8a000` |

- **通常ピースは黒インクの点描**。**ハザードだけ彩度の高い自己発光色**にして「盤面に紛れ込んだ異物」感を出す
  （例: ブラックホール＝血赤、ワームホール/レンズ/霧＝紫、彗星＝空色/赤、CME＝橙）。
- 描画は**ドットアート（点描・小さな `fillRect`）で統一**。新ギミックも同じ点描スタイルで描くこと。
- **クリーム地は塗り替えない**。ギミック自身を彩度色のドットアートで描くことで宇宙感を足す。

### 再利用する描画ヘルパー（`src/components/CryptoPeggleGame.tsx`）
| ヘルパー | 用途 |
|---|---|
| `drawDots(ctx, dots, cx, cy, rot, frame, color, alphaMult)` (L428) | ドット雲を1色で描くコアレンダラ |
| `drawSolidCircle(ctx, x, y, r, color)` (L451) | アンチエイリアスされた実体コア（核・惑星など） |
| `circleTrig(n)` (L417) | 等間隔リングの cos/sin メモ化（回転リング・降着円盤に） |
| `spawnBurst(g, x, y, vx, vy, color?)` (L605) | 衝突スパーク（色引数対応・速度スケール） |
| `makePegDots(type)` (L225) | 新ペグ型のモチーフ（点の配置）を定義 |
| `makeDot(x, y, sizeW)` (L214) | 点描の最小単位 |
| `bakeFogCloudSprite(cloud, dpr)` (L515) | 重い霧系はオフスクリーンに1度ベイクする性能パターン |

### 親ルールの継承
- 親 `../CLAUDE.md` の **OG画像 / Satori 実装ルール**（em dash `—` 禁止、全 `<div>` に `display:flex`、
  `radial-gradient`/`box-shadow` 非対応 等）は**このプロジェクトでも有効**。重複コピーせず親を参照する。

---

## 3. 現在の宇宙ギミック進行表（実装済み＝ベースライン）

| ギミック | 解禁Lv | 物理/効果 | 配色モチーフ |
|---|---|---|---|
| ブラックホール/重力ゾーン | 7 | 半径引力・内側で吸収 | 血赤〜栗色の渦＋事象の地平線 |
| ワームホール | 9 | 一定周期でテレポート（対で出現） | 紫のバー＋もやオーラ |
| 青彗星 | 12 | 触れると跳ね返す（動くバンパー） | 空色の尾＋濃紺の核 |
| 重力レンズ | 15 | 接線（渦巻き）で軌道を湾曲 | 紫の回転リング |
| 霧（唯一の背景系） | 17 | 視界を宇宙の紫もやで遮る | 暗い宇宙紫＋TV砂嵐 |
| 赤彗星 | 18 | 触れた玉を消滅（横断して退場） | 赤い尾＋赤警告 |
| CME（コロナ質量放出） | 20 | 上→下へ掃引・玉を下＋外へ押す | オレンジのプラズマ帯 |
| パルサー | 24 | 回転する双子ビームが玉を外向きに押す（放射圧） | シアン/白のビーム＋白熱核 |
| 重力波 | 27 | 周期的な波紋リングが通過時に玉の速度を回転（速度保存の曲げ） | 銀灰のリップル＋連星の震源 |
| 真空崩壊バブル | 29 | 成長する泡の内部で重力反転（正味0.5倍上向き）。上限でポップ→再生成 | 不気味な緑の膜＋上昇する微光 |

- **アノマリー・スペシャル**（2026-07-13 追加）: 5の倍数（非ボス）のレベルは通常ロールの代わりに
  厳選構成が出る — 彗星群(常時)/双極(lv30+)/群棲(lv40+)/静寂(lv50+)/赤の日(lv60+)/
  **符号反転の日(lv200+)/校正崩壊の日(lv200+)**。
  generateLevel 末尾で全ロール後に専用 `anomalyRng` で差し替え（既存レイアウトの決定論に無影響）。
  **新ハザード配列を追加したら generateLevel 末尾のアノマリー空化リストにも必ず追加すること。**
- **ゾーンリミックス**（2026-07-16 追加）: 解禁Lvでもアノマリーでもないギャップレベルで、専用
  `remixRng` により25%で現在ゾーンのハザード2種を強制共存（Zone K 実装済。排他尊重・未解禁は除外）。
  深い盤が「浅いハザードだけのスープ」になるのを防ぐギャップ充填。
- **挙動バリアント**（2026-07-14 追加・Batch I/J）: 連星彗星(lv45+)/ワームホール三連(lv47+)/
  二重逆回転レンズ(lv48+)/往復赤彗星(lv50+)/真空バブル対(lv52+)/三条パルサー(lv54+)/
  脈動BH(lv60+)/漂うマグネター(lv63+)、各40%。変種ロールは**対象ハザードの spawn 分岐内で
  同ストリームから消費する作法**（オーナー決定）。既知のトレードオフとして、変種を追加した
  レベル帯の盤面は版間で変わる（同一ビルド内の決定論とメイン rng の消費順は不変なので、
  レビューで blocking 扱いにしない）。
- **挙動バリアント・レア**（2026-07-21・Batch R1〜）: I/J 未対応ハザードに **20%** レア。
  R1: 霧の裂け目(lv28+)/斜めCME(lv30+)/脈動WH(lv38+)/双震源GW(lv42+)。
  R2: 楕円ローグ(lv42+)/双極クエーサー(lv43+)/双子マイクロBH(lv44+)/逆エルゴ contra(lv46+)。
  R3: 二重リコネクト(lv47+)/双子前駆星(lv48+)/潮汐位相反転(lv49+)/漂うボイド(lv50+)。
  R4: 双アクシオン(lv52+)/双FRB(lv53+)/対フレック(lv54+)/二重ハロー(lv55+)/脈動DE域(lv56+)。
  R5: 双弧潮汐流(lv60+)/脈動時間膨張(lv61+)/二重鏡像環(lv62+)/凍結特異点(lv64+)/双ORC(lv65+)/
  双極TDE(lv66+)/逆ダークフロー(lv67+)/両壁アトラクター(lv68+)。
  R6: 分岐ラニアケア(lv70+)/直交せん断(lv71+)/位相反転BAO(lv72+)/反転弾丸(lv73+)/
  逆相ハム(lv74+)/逆複屈折(lv74+)/二重コースティック(lv75+)。
  霧・ダークフローは initLevel の Math.random（本体と同系）。他は spawn 同ストリーム。
- **新ペグ型**（2026-07-14 追加・Batch K）: 中性子(lv28: 2ヒット重減衰＋effMinSpeed再クランプ)/
  対生成(lv31: 消すと近傍に青ペグ1個を後追い生成)/量子もつれ(lv34: ペアで同時消滅・各10点)/
  赤方偏移(lv38: レベル経過フレームで得点漸減45→8・インクが赤銅色へ)。全て **blue プールから変換**
  （mud の後に append＝gimmickRng は末尾消費で既存ペグ選択に無影響、blue 相当なので orange
  クリア条件に無関係＝詰み厳禁を満たす）。カスケード（爆弾/雷）に巻き込まれた場合は汎用10点として
  処理され、特殊効果（partner消滅・青生成）は**直接ヒット時のみ**発火する仕様。
- 参考: 以前は「ペグ系は lv25/26 で打ち止め」だったが、Batch K で lv28/31/34/38 に4種追加した。
- **新しい宇宙ギミック（ハザード）は原則 lv22 以降**に足していくことで「深部ほど宇宙感が増す」を実現する。

---

## 4. 進行の原則（Progression Rules）— 新ギミック追加時に必ず守る

1. **深いレベルほど exotic に** — 追加は原則 lv22+。より未知・強力・予測困難なものほど深いレベル帯へ。
2. **プレビュー無反映＝フェアな難しさ** — `computeTrajectory` は**変更しない**。引力・湾曲・押し出しは
   狙い線（トラジェクトリ・プレビュー）に反映させない＝「読めない宇宙」がそのまま難易度になる。
3. **決定論を保つ** — 生成は専用 `makeRng((rng()*0x100000000)>>>0)`（既存の `hazardRng` 等の作法）で
   シードし、メインの `rng` 系列を乱さない。
4. **詰み厳禁** — 新ギミックは**クリア条件（orange 全消し／ボス撃破）に無関係**にする（玉を消す/押す/曲げるだけ）。
   重力は毎フレーム加算され、`STUCK_FRAMES` 救済もあるので、恒久 stuck（安定周回）を作らないこと。
5. **レベルで強度スケール** — 解禁ゲート＋確率ランプ＋数/強度の漸増（既存ハザードと同じ作法）。
6. **クリーム地を保つ** — 背景は塗り替えない（§1・§2）。

---

## 5. 宇宙ギミック・ログ（今後ユーザーが決めて追記）

具体的なギミック案は**ここには事前列挙しない**（内容・採否は都度ユーザーが決定）。
未知の宇宙現象を1つ決めて実装するたびに、下表へ1行追記して他スレッドと状態を共有する。

> 候補カタログ: **[docs/COSMIC_GIMMICK_SPEC.md](docs/COSMIC_GIMMICK_SPEC.md)**（47案の実装仕様つき、
> レベル帯→宇宙の深度の対応は同ファイル §2.0 深度マップ参照）。
> 採否はそのファイル §4 の採否ログに記入し、実装したらこの下の表にも1行追記する。

### 選定基準（「良い宇宙ギミック」の条件）
- 未知で異様な**宇宙現象**であること。
- §4 の進行原則を満たす（詰み無し・決定論・プレビュー無反映・クリーム地維持）。
- 既存の**彩度ドットアート＋再利用ヘルパー**（§2）で描けること。
- **深いレベルほど exotic**（原則 lv22 以降、特に打ち止め lv25/26 以降に足す）。

### ログ（実装時にこの表へ追記）
| ギミック | 解禁Lv | 効果 | 配色モチーフ | コミット |
|---|---|---|---|---|
| パルサー | 24 | 回転双子ビームの放射圧で玉を外向きに押す（フォース型） | シアン/白 | （未コミット） |
| 重力波 | 27 | 膨張する波面帯の通過中、玉の速度を回転（速度保存） | 銀灰/白金 | （未コミット） |
| 真空崩壊バブル | 29 | 泡内部で重力反転（浮力）。rMax でポップ→110f 後に再成長 | 不気味な緑 | （未コミット） |
| ホワイトホール | 23 | BHの逆＝半径斥力で玉を押し返す（吸収なし・フォース型） | 氷青/白熱核 | （spec #1） |
| マグネター | 31 | 周期フレア（予告→放出6f）で範囲内の全玉を外向きに弾く（パルス型） | 鉄灰核/アンバー放電 | （spec #2） |
| ローグプラネット | 32 | 盤面を漂う移動重力井戸＋実体バウンス（スリングショット） | 暗い藍鼠/淡灰リング | （spec #3） |
| クエーサージェット | 33 | 固定プラズマ柱で柱内の玉を軸方向へ加速＋横噴射（フォース型） | 菫プラズマ/白熱ノズル | （spec #4） |
| 蒸発マイクロBH | 34 | 縮小する引力→蒸発で斥力バースト→別地点に再出現（フォース＋パルス） | 血赤渦/白ホーキング放射 | （spec #5） |
| ダークマター・ハロー | 48 | ほぼ不可視の弱い引力源。周期シマーだけが手がかり（フォース型） | 淡い藍のゴーストリング | （spec #6） |
| エルゴ球 | 36 | 回転BH周囲のリング帯だけに一方向の接線ドラッグ＋弱い内向き成分（フォース型） | 深紫の異速二重リング/黒コア | （spec #7） |
| 磁気リコネクション | 37 | 平常時は無力なX字磁力線。周期スナップ(8f)で線上の玉を交点から遠ざける向きに射出（パルス型） | マゼンタの十字線＋白熱スナップ | （spec #8） |
| 超新星前駆星 | 38 | 実体バウンス(r:14→30が視覚と当たり判定で完全一致)。最大時に爆発→収縮→再膨張の周期（接触衝突型＋パルス型） | 赤橙の対流巨星＋白熱爆発 | （spec #9） |
| 潮汐ストレッチ場 | 39 | 速度を半径/接線成分に分解し半径を増幅・接線を減衰（BALL_SPEED*2でクランプ）。フォース型 | 鋼青の放射ストリーク | （spec #10） |
| タキオン流 | 88 | 盤面を貫く斜め帯（along方向は無制限）。帯内で常時加速（BALL_SPEED*2クランプ）。フォース型 | 白い高速ストリーク（尾が先・頭が後）＋琥珀の縁 | （spec #11） |
| 宇宙ボイド | 42 | 楕円内で重力半減＋微ドラッグ（0.995）。effMinSpeedをfreeze/mud同様に抑制。場の修飾型 | ほぼ無描画＋境界の淡いグレー点線 | （spec #12） |
| アクシオン壁 | 43 | gone→fadeIn→solid→fadeOutを巡回するOBB膜。solid中のみバンパー式反射。接触衝突型・時限 | 真珠白＋微虹のドット膜 | （spec #13） |
| FRB | 44 | 周期発火時、全玉の速度を±0.30rad一斉回転（速度保存）。盤面全域パルス型 | 水色の同心リング3本＋端の明滅発信源 | （spec #14） |
| 反物質フレック | 45 | 漂う微小地雷。接触で玉を消滅→30f休眠→新位置へ再形成。赤彗星と同時生成しない。接触消滅型 | 黒リング＋白コア（白黒反転） | （spec #15） |
| 量子トンネル障壁 | 84 | 初回接触でMath.random()<0.5を1回だけ判定→反射 or 完全すり抜け（WeakSetでロック）。接触衝突型・確率 | 藍色の点滅する点線バリア | （spec #16） |
| 時間膨張場 | 52 | 侵入/退出の遷移フレームで速度0.5倍/2倍（Ball.dilatedで検知）。重力も場内は半分。場の修飾型 | 琥珀の同心円弧（最遅回転） | （spec #17） |
| 宇宙ひも | 86 | 傾いた1px線分を横切ると線の軸方向へシフト量だけ瞬間平行移動（速度不変）。WeakSet&lt;Ball&gt;で1横断1回をロック。接触衝突型（テレポート系） | 白熱線＋結び目ドット | （spec #18） |
| ダークエネルギー膨張域 | 49 | 距離に比例して強くなる斥力（ホワイトホールの逆プロファイル）。範囲端で強制カットオフ。連続フォース型 | 淡いドット格子の膨張ループ＋桃の点線境界 | （spec #19） |
| 銀河潮汐ストリーム | 51 | 円弧の帯（半径±26px、角度範囲~100°）内で接線方向のみの一方向力。半径成分なし＝レンズより詰みにくい。連続フォース型 | 白〜淡金の星ドット流 | （spec #20） |
| アインシュタイン鏡像リング | 52 | リング線(r=60固定)横断で速度を接線軸鏡映反転(法線維持・接線反転、速度保存)。WeakSet&lt;Ball&gt;で1横断1回。接触リング型 | 銀白リング＋輝点2つ周回 | （spec #21） |
| 裸の特異点 | 53 | 接線力の向きが`sin(3θ+frame*0.02)`で乱流反転(3乗減衰)＋常時外向き0.2倍バイアスで排出保証。35%ロール。連続フォース型・カオス | 血赤/白の壊れた渦＋周期的1f完全静止 | （spec #22） |
| ハイパーベロシティ・スター | 66 | 彗星の予告/横断/退場を流用、実体バウンスなし。背後120px×半幅30の後流帯で玉を進行方向へドラッグ(f=0.5*t*t)。移動体＋連続フォース型 | 白熱核＋前方青方偏移尾/後方赤方偏移尾 | （spec #23） |
| さまよえるブラックホール | 69 | 本家BHの引力式(f=0.6*t*t, R=120)+吸収r=10を流用、中心がリサージュ移動(振幅70/45、周波数0.006/0.0043)。井戸自体が動くため安定周回不可。移動する連続フォース型 | 血赤渦の縮小版＋降着ドットの尾 | （spec #24） |
| オッドラジオサークル | 56 | 重力波の超スロー版。r=60→260を約900fで膨張、帯半幅18で外向きf=0.35*t*t。フェード20f→再凝縮20fの3状態機械。重力波と同一レベル非配置。連続フォース型 | 淡藤色の半透明リング＋触れた弧のみ点灯 | （spec #25） |
| 潮汐破壊イベント | 57 | R=130で内巻き渦(接線0.4*t*t+内向き0.15*t*t)。dist<30で強制上向きジェット(vy-=1.3,vx*=0.9)がvortex項を上書き。連続フォース型・複合 | 白い内巻きスパイラル＋暗核＋上向き光柱 | （spec #26） |
| ダークフロー | 58 | 盤面全体・全球に常時作用する回転方向の弱い加速(基準0.012、レベルで0.03まで漸増、θ=θ0+frame*0.0004)。風と排他(45%ロール、initLevelで風の後にMath.random()判定＝generateLevel非依存の唯一のハザード)。背景ドットにも同方向バイアス。連続フォース型・全域 | 背景ドットの一方向ドリフト＋淡い縁の流線 | （spec #27） |
| グレート・アトラクター | 59 | 引力源が画面外(-140 or W+140, y=H*0.4)固定点という初の点引力(吸収なし)。f=0.22*t*t(R=W)に呼吸係数0.5+0.5*sin(frame*0.01)を乗算。詰み回避は呼吸ではなく重力(0.20/f)が近壁での最大鉛直引力成分を上回ること。45%ロール。ゾーンA(lv54-59)最後のギミック | 近壁の暗い回避帯＋壁へ加速しながら消える塵流線 | （spec #28） |
| 弾丸クラスター | 64 | 1ハザードに連続フォース型(見えないDM塊、R=110・f=0.3*t*t・吸収なし)と接触衝突型(見えるガス塊、r=26・彗星式バウンス+運動量0.6キャリー+effMinSpeedクランプ)を同居させた初の複合ハザード。DM位置はガス塊のvx符号方向に60px先行(dmX=bc.x+sign(vx)*60)で毎フレーム導出。実機検証でDM牽引→ガス衝突(hitFlash発火)の順序を確認。45%ロール。ゾーンB(lv61-65)最初のギミック | ピンク橙の衝撃波弧+尾を引く熱いガス塊(DM塊は無描画、60fに1回1pxシマーのみ) | （spec #29） |
| バリオン音響振動 | 63 | 静的な同心3リング(半径55/110/165)。各リング±6pxで呼吸(位相120°ずつずらし、内→外へ脈打つ)。リング線±14px帯内で法線方向のみの引力(外側なら内向き、内側なら外側、t*t減衰・接線成分ゼロ)。生成後は生滅なし(永続)。45%ロール。ゾーンB(lv61-65)2番目のギミック | 砂色の同心円ドット+触れた弧だけ発光 | （spec #30） |
| ラニアケア流域 | 61 | 3本の二次ベジェ曲線(始点は盤面中心から0.55〜0.80倍半径、終点は共有の画面端シンク点)を生成時に24点のポリラインへ事前計算。新規汎用ヘルパー`closestOnPolyline`で最近傍セグメントへの距離+接線方向を取得し、帯内(±30px)で純粋に接線方向の力(f=0.25*t*t)のみを適用(シンク自体への引力なし=詰み不可、到達は通常の壁バウンス)。実機検証：L字型ポリラインで折れ線部分に玉を置きvx=0を維持したまま(垂直区間の接線は純鉛直)vyのみ増加することを確認、シンクへの求心力混入がないことを証明。45%ロール。ゾーンB(lv61-65)3番目のギミック | 極淡青灰の点列流線3本+疎らな「銀河」ドット、シンク近くでフェードアウト | （spec #31） |
| 背景重力波ハム | 64 | 専用interfaceを持たない最小実装(cmeActiveと同型の単一boolean)。全球・毎フレーム・無条件に速度回転(Δθ=A*sin(frame*0.07+ballIndex*2.1)、A=0.003+(lv-64)*0.0004、速度保存の回転行列)。重力波(lv27波面型)と同一レベル非配置(gravWaves.length===0)。実機検証：回転式を単体テストしspeed diff=0(浮動小数点誤差内)を確認、実機では重力の加速と合成されるため速度は自然に増加(回転自体は無関係)。ボールindexOfでball毎に異なる位相を確認。45%ロール。ゾーンB(lv61-65)4番目のギミック | 画面四隅の4点が完全同相で明滅するのみ(専用天体なし) | （spec #32） |
| 宇宙複屈折シート | 65 | 傾いたOBBシート(200×90)を玉が無抵抗で通過、遠い面から抜けた瞬間だけ速度回転(表→裏+0.22rad、裏→表-0.22rad、決定的・速度保存)。時間膨張場と同様にBallへ新フィールド`bfSide`を追加(3箇所のBall生成サイト全てに`bfSide: 0`を追加済み)。シートのOBBを抜けると`bfSide`は0にリセット(再突入は必ず「初回接触=記録のみ」から)。実機検証は実機のrAFタイミングが環境により秒間数千フレームまで加速することが判明したため、物理ブロックのロジックをそのまま複製した単体シミュレーションで検証：1回だけ発火・速度保存(diff=0)・裏→表遷移で-0.22rad(仕様通り)・同じ面から出入りしても誤発火なし、の4点を確認。40%ロール。ゾーンB(lv61-65)最後のギミック（#29〜33で完結） | 薄紫の平行縞ドット(偏光格子)+横断時に十字マーカーが回転しながら10fでフェードアウト | （spec #33） |
| リトル・レッド・ドット | 68 | 4〜6個の静止小赤点(r=6)、各点が独立位相で120f点灯⇄90f消灯を周期。点灯中のみ実体バウンス(effMinSpeedクランプ・運動量キャリーなし)+微引力(R=60,f=0.25*t*t、hazardAgeBoostで深部微増)、消灯中は物理すり抜けだが1px赤ゴーストは残す。フェードは暈先行/核後追いの非対称。赤彗星と同時生成しない。45%ロール。ゾーンC入口手前 | 濃赤コア+濃い電離ガス繭(暈)+消灯時1pxシード | （spec #34） |
| 原始ブラックホール群 | 76 | 3〜5個の不可視微引力点(各R=70,f=0.30*t*t,吸収なし)を星座状に配置。生成時に最小間隔120pxを保証する棄却サンプリング(最大200試行、上限に達しても部分配置で安全に終了)。各点は独立位相で約140f周期に1回だけ1pxシマー(3f)。レビューでPBH_FORCE(0.30)がGRAVITY(0.20)より大きいことが判明したが、吸収が一切ない連続力+stuck-rescue(220f)が実質的な詰み回避の正体であり安全（「点間の綱引き」は2点の70px圏が重なる約20px幅の狭い帯でのみ意味を持つ）。実機検証はrAFタイミングの不安定さを避け、棄却サンプリングを500シードで単体シミュレーション：全シードで要求数を配置成功(最大試行数17/200)・最小間隔違反ゼロを確認。45%ロール。ゾーンC(lv71-77)2番目のギミック | ほぼ無描画、各点1pxのシマーのみ | （spec #35） |
| ダークスター | 73 | 本作初のバウンスしない大質量天体(R視覚=48)。内部(dist&lt;40)はv*=0.99ドラッグのみで重力は抑制しない＋effMinSpeedをBALL_SPEED*0.35に抑制。表殻(40〜60)は外向き放射圧(f_peak=0.30、帯中央50でピーク・端でゼロの二乗減衰)。宇宙ボイド/時間膨張場との共有パス(effMinSpeed三項・重力/ドラッグ区画)は既存分岐の後ろに独立ブロック追加のみでリグレッションなし。宇宙ボイドと生成座標が重なり得るが詰みは発生しない(非ブロッキング)。仕様の「内部玉周囲の明滅半速」視覚演出は未実装(任意)。単体シミュレーションで(a)中心から24fで殻脱出・終端速度≈DS_DRAG*g/(1-DS_DRAG)=19.8に収束(b)殻力は40〜60帯のみ(c)core/shell境界に重複ゼロを確認。45%ロール。ゾーンC(lv71-77)3番目のギミック | 淡橙白のふわふわ点描球＋内部白スパーク＋表殻外向き微流 | （spec #36） |
| CMBゆらぎシート | 74 | 盤面全域の温度地図T(x,y)=sin(x*0.030+φ1)*cos(y*0.024+φ2)+0.5*sin(x*0.011-y*0.017+φ3)。`ball.vy -= 0.020*T`（ホット=上昇・コールド=下降）。最大揚力≈0.030≪GRAVITY0.20で平衡点なし。点群は生成時ベイク、毎フレームはalpha変調のみ(k=0.005)。`cmbAnisotropy: CmbAnisotropy|null`（gwBackgroundActive型の最小実装）。単体simで20開始位置すべてが600f以内に脱出・stalled=0・gravityDominatesを確認。40%ロール。ゾーンC(lv71-77)4番目のギミック | 暖点#e8c8a0/冷点#a8c8e0の極淡斑ドット(alpha≤0.10) | （spec #37） |
| ホーキング・ポイント | 75 | 亡霊輪(r=40)1〜2個。周期300fで10fだけ外向き温もりパルス(f=0.8*t*t,R=120)。平常時は完全無力。マグネターと同型のtimer/releaseTimer状態機械（drawでadvance・forceはreleaseTimer&gt;0のみ）。パルス30f前に2f完全消灯。重力波/ORCと同一レベル非配置(リング族排他)。単体simでパルス周期・blink=2f/pulse・力帯プロファイルを確認。40%ロール。ゾーンC(lv71-77)5番目のギミック | 平常時alpha0.10〜0.15の亡霊輪#d8d0c0＋パルス時白熱衝撃波 | （spec #38） |
| 宇宙の暗黒時代 | 77 | 霧の最終形＝暗黒。物理なしの視界ギミック。不透明黒の暗幕に発射台・生存玉・接触ペグ/ハザード周辺のソフトライト穴を開け、エイム軌道・発射台・玉を再描画。霧と同一レベル非配置。フェードイン30f。ゾーンC(lv71-77)最後 | 真っ暗＋局所照明（クリームの発射台/エイム/玉） | （spec #39） |
| 量子泡 | 81 | 領域R=100内で毎フレーム速度をΔθ=0.03*sin(frame*0.31+ballIndex*1.7)回転（速度保存・平均ゼロ）。描画時のみ表示座標を2pxグリッドにスナップ（実座標は連続）。単体simで500f速度diff≈0・平均回転≈0を確認。40%ロール。ゾーンD(lv81-87)最初のギミック | 白黒1px対生成ドット＋不定形点線境界 | （spec #40） |
| ファイアウォール | 83 | 円弧バリア(r=80・中心角120°・半幅5)。接触で半径法線反射＋±0.6radスクランブル(hashペグ式)＋effMinSpeedクランプ。hitCool8f。サブステップ衝突必須。単体simで弧ヒット率=1/3・速度保存・下向きバイアスを確認。40%ロール。ゾーンD(lv81-87)2番目のギミック | 白⇄橙高速フリッカー炎弧＋0/1ビット流 | （spec #41） |
| 超放射 | 85 | 引力f=0.4*t*t(R=120)＋接線加速+0.10。周回で増速→遠心脱出（捕獲構造的に不可能）。BALL_SPEED*2クランプ。軌道1周で白増幅波(12f)放出＋spin*=0.98(下限0.5)。エルゴ球と同一レベル非配置。単体simで400f以内脱出・maxSpd≤16を確認。40%ロール。ゾーンD(lv81-87)3番目のギミック | 血赤渦＋白増幅波紋＋スピン減衰 | （spec #42） |
| 負質量ブロブ | 87 | 最寄り玉へ0.6px/f追尾＋R=100外向き斥力f=0.45*t*t。実体なし・画面端で停止。BALL_SPEED*2クランプ。斥力のみ＋追尾≪押し出しで捕獲不可。単体simで300f捕獲ゼロ・minSep&gt;48を確認。35%ロール。ゾーンD(lv81-87)最後のギミック | 白抜き輪郭点線の「穴」（内部はクリームより僅かに明るい） | （spec #43） |
| 泡宇宙の衝突痕 | 91 | 大円R=150。内部で重力を±18°傾け0.85倍（生成時に向き決定）。域外は通常重力のみ（二重適用なし）。Ball.bucFlashを3スポーン全てに追加。単体simで中心から脱出・下向きを確認。40%ロール。ゾーンE(lv91-99)最初のギミック | 桃/水色干渉縞リング＋青ずれ内点＋進入/退出虹縁 | （spec #44） |
| ビッグリップ前兆 | 93 | 周期400fで20fの全域膨張パルス。f=H_rip*dist（中心から、遠いほど強い）。Hは0.004開始・イベント後×1.15（上限3倍）。ダークエネルギー域と排他。四辺裂け目の予告/漸増。単体simでH上限到達・エッジ玉脱出を確認。40%ロール。ゾーンE 2番目 | 背景ドットの空間伸縮＋四辺白裂け目 | （spec #45） |
| 共形サイクリック境界 | 95 | 下端帯で未再誕玉を上端へ1回再誕（速度保存・Ball.reborn/goldTimer）。虹色地平線＋縦ストリーク＋金残光。単体simで1回目再誕・2回目退場・H+100中盤死は非再誕を確認。40%ロール。ゾーンE 3番目 | 極淡虹色地平線＋白/金産声 | （spec #46） |
| 無 | 99 | R=110円内で重力含む全連続フォース不在（直線等速）。stuck凍結。bgDots円欠のみ・装飾ゼロ。生成時領域内ペグ除去。単体simで直線脱出・rescue非発火を確認。35%ロール。ゾーンE最後（カタログ完走） | 何も描かない（インク欠のみ） | （spec #47） |
| 宇宙せん断場 | 62 | 楕円内で進行角を固定せん断軸へ速度保存回転（最大0.012rad/f・t²ランプ）。中心引力なし・通常重力維持。孤立simで16方向の速度保存/収束、24開始条件すべて離脱を確認。45%ロール | 青緑/珊瑚の整列銀河ドット＋作用中の近傍3個点灯 | `8fbfa64`（spec #48） |
| 無衝突衝撃波 | 67 | V字前縁横断で接線保持・法線屈折増幅（反射/吸収なし）。サブステップ＋WeakSet1横断1発火。BALL_SPEED*2/effMinSpeedクランプ。40%ロール | コバルトV字2px＋ピンク乱流尾＋白先端閃光 | `20a07ac`（spec #49） |
| シルク減衰雲 | 72 | 楕円内で短軸成分のみ連続減衰（0.975/f）。長軸速度保存・重力維持・速度床BALL_SPEED*0.38。40%ロール | 銅金/スレート青の短軸スミア点群 | `2564fbf`（spec #50） |
| プランク回折格子 | 82 | 遠い面退出時に速度を5離散回折角へ量子化（速度保存）。Ball.pdgSideで表裏追跡。40%ロール | 藍2pxスリット＋橙次数線＋白横断コア | `54009f6`（spec #51） |
| 真空チェレンコフ領域 | 89 | 高速玉のみ12f間隔でチェレンコフ減速＋交互リコイル。Ball.vcTimer/vcFlip。35%ロール | 飽和シアン境界＋紫優先軸＋発火円錐 | `094e5d6`（spec #52） |
| 閉じた時間的曲線 | 97 | 輪帯初回進入48f後に位置速度を1回復元。WeakMap/WeakSet。30%ロール | マゼンタ/青緑二重リング＋アンカー予告 | `094e5d6`（spec #53） |
| 重力レンズ・コースティック | 65 | 折り畳み線横断で法線成分1.35倍（接線保存）。WeakSet1横断1発火。40%ロール | 金2px折り畳み線＋白折り点 | `cbf573b`（spec #54） |
| 再電離前線 | 71 | CME型の移動UV前線。帯内vx*=0.97・vy+=0.06。40%ロール | 紫#7b5cff前線＋淡灰ウェイク | `4fa3063`（spec #55） |
| ニュートリノ振動 | 78 | 楕円内で味振動=速度保存微回転。40%ロール | 藍鼠3層ハロー | `b7ff9d7`（spec #56） |
| 重力波記憶 | 85 | 遅い膨張波面+90f残滓バイアス。gravWaves排他。40%ロール | 銀灰リング＋歪み尾 | `3b71f2f`（spec #57） |
| 愛因シュタイン十字 | 94 | 核+4像の合成弱引力。45%ロール | 琥珀4像＋暗琥珀核 | `ca68bdb`（spec #58） |
| 量子ゼノ観測域 | 98 | 観測duty開放中のみ速度0.93倍。theNothings排他。35%ロール | 青緑観測孔＋暗スポーク | `bbbb84d`（spec #59） |
| Paper Moods | 40+ | レベルごと紙の癖（主ムード＋稀レア）。物理なし | 塵の挙動・縁点列のみ | （docs/PAPER_MOODS.md） |
| 超越太陽質量チャープ | 100 | 双星チャープで速度振幅±8%変調（方向保存）。35%ロール | 深緑青双星＋淡軌道 | `fef0fd0`（spec #60） |
| ぼんやり暗黒物ソリトン | 104 | 楕円内接線干渉ビート。40%ロール | ソリトンミント同心リング＋干渉縁 | `2f3ec65`（spec #61） |
| アクシオン星ミニクラスター | 108 | 不可視点の接線sin干渉。40%ロール | 幻影藍シマー＋子点 | `585fbc4`（spec #62） |
| 宇宙論的视界エントロピー流 | 112 | 四辺帯から内向き斥力。greatAttractor排他。40%ロール | 熵錆辺＋内側流線 | `4553f24`（spec #63） |
| ホログラフィックRGシート | 116 | OBB層通過でrgLayer速度スケール。35%ロール | 真珠平行縞＋層オフセット | `dfe682e`（spec #64） |
| 質量-视界エントロピー減速 | 119 | 中心距離比例の全域微減速。bigRip排他。35%ロール | 深葡萄酒放射24本 | `d83a812`（spec #65） |
| Pop III.1 フラッシュ再電離 | 122 | 同期電離パッチの外向きフラッシュ→再結合ドラッグ。reion/fog排他。40%ロール | 紫外白フラッシュ＋錆灰再結合 | `54b7a3c`（spec #66） |
| 逃走SMBH・バウショック | 126 | 移動先端のV字バウ押し＋ウェイク減速。rogueBHs/赤彗星排他。40%ロール | 緑青先端＋V弧＋星形成ウェイク | `bf8ee47`（spec #67） |
| ファントム交差膜 | 130 | 帯横断でwSign反転＋帯内中線フォース。bigRip/DE域排他。35%ロール | 琥珀/深紫交互シーム | `c9b4bdf`（spec #68） |
| レンズ異常場 Alens | 133 | 全域速度保存微回転。gravWaves/gwBackground排他。35%ロール | 四隅灰紫スポーク＋玉fxTwist | `5be9359`（spec #69） |
| ビッグリング uLSS | 136 | 大環接線流のみ（半径なし）。ORC/BAO排他。35%ロール | 錆シアン疎点描リング＋玉fxTrail | `2035ef5`（spec #70） |
| パッチ状 kSZ キック | 139 | 楕円内固定軸パルスキック。#66同レベル非配置。35%ロール | 冷シアン境界明滅＋軸ストリーク | `05d475d`（spec #71） |
| 太陽未満 PBH エコー合体 | 142 | 二点弱引力が接近→10f重力ヌル→再凝縮。microBH/PBH排他。35%ロール | 錆鉄二重点＋エコー灰欠け環 | `dd99d19`（spec #72） |
| Quintom 呼吸重力 | 146 | 全域重力が極低速で0.88↔1.12呼吸。bigRip/phantom/DE排他。35%ロール | 四辺琥珀/深紫位相縁＋玉FX | `6cd96d3`（spec #73） |
| ブラックホール星・繭 | 150 | 殻帯ドラッグ＋周期裂け目の外向きパルス。littleRedDots排他。35%ロール | 濃赤核＋銅金繭＋裂けアーク | `8d3d277`（spec #74） |
| 二重 H₀ 縫い目 | 153 | 斜め二帯で重力1.10/0.90＋横断±0.08radねじれ。alens/gwBackground排他。35%ロール | 破線縫い目＋密度差塵＋玉FX | `2d13bc5`（spec #75） |
| Hellings–Downs 相関ハム | 156 | 基準玉との角距離でHD相関ねじれ。gwBackground/alens/gravWaves排他。35%ロール | 四隅位相差点＋玉fxTwist | `0064485`（spec #76） |
| SIDM 最終パーセク・スパイク | 159 | 固定二核＋核間接線摩擦＋弱内向き。chirp/bullet排他。35%ロール | 錆シアン流線＋二重核 | `845e861`（spec #77） |
| ニュートリノ質量ヌル帯 | 162 | 帯内重力0.55＋微ドラッグ。Quintom/dualH0排他。35%ロール | 灰緑欠け点線＋玉fxField | `a779a8e`（spec #78） |
| 二成分DM質量偏析 | 165 | 内殻弱内向き＋外殻弱外向き。sidm/darkHalo/PBH排他。35%ロール | 濃紺核＋疎灰環＋玉FX | `995146f`（spec #79） |
| 自由流平滑場 | 168 | 楕円内で速度を前フレームと混合平滑。silk/foam排他。35%ロール | スレート点線楕円＋玉fxTrail | `71c40a3`（spec #80） |
| 見かけ過大質量核 | 171 | 弱引力＋周期バーストで見かけ暴き。bhStar/microBH排他。35%ロール | 錆紫過大繭＋バースト核 | `6d7a98c`（spec #81） |
| FRBマイクロレンズIMBH | 174 | コーシック弧横断でキック＋ねじれ。axionML/caustic排他。35%ロール | 1pxシマー＋二重ゴースト弧 | `c4af4f5`（spec #82） |
| 原始磁場バリオンクランプ | 177 | 多核弱凝集＋外帯微斥力。cmb/pop31排他。35%ロール | 磁灰疎点クラスタ＋玉FX | `0e097fa`（spec #83） |
| IDEエネルギーサイフォン帯 | 182 | 帯内dwellで重力増＋外向き微力減衰。Quintom/nuNull/dualH0排他。35%ロール | 琥珀灰欠け点線＋玉FX | `2b6e29f`（spec #84） |
| 運動量のみダーク結合 | 185 | 玉ペア接線速度を弱揃え（加速総和≈0）。hdHum/alens/gwb排他。35%ロール | 四隅位相差点＋玉fxTwist | `1a148b0`（spec #85） |
| 真空崩壊リーク | 188 | 静止円の周期弱内向き（sin²）→40f無力。vacuums/bigRip排他。35%ロール | 青白欠け環＋玉FX | `7b3c4db`（spec #86） |
| 重力エコー遅延 | 191 | 震源の過去摂動を遅延微回転で着弾。hdHum/alens/GW族排他。35%ロール | 薄藤遅延弧＋玉fxTwist | `493e6b6`（spec #87） |
| ボソン星ソフトコーシック | 194 | 拡張レンズ縁で進行角折り（内部無力）。frbML/axionML/caustic排他。35%ロール | 淡金二重縁＋玉FX | `b0224dc`（spec #88） |
| 固有整列汚染場 | 197 | 偽せん断で進行角を固定軸へ揃える。alens/shear/hdHum排他。35%ロール | 灰紫平行ストリーク＋玉fxTwist | `e0a0c57`（spec #89） |
| 符号反転IDE縫い目 | 202 | 縫い目で弱力の向きが反転（sides/timer）。サイフォン族排他。35%ロール | 錆氷二色欠け点線＋玉Trail/Field | `61cfa9a`（spec #90） |
| ファントム横断帯 | 208 | 水平帯横断で重力スケール0.90/1.10。phantom/Quintom/bigRip排他。35%ロール | 灰紫欠け横帯＋玉Twist/Field | `0c06a43`（spec #92） |
| 乗法シアバイアス幕 | 211 | 楕円内6fごと速さのみ±3%誤校正。ia/alens/shear排他。35%ロール | 灰シアン校正目盛り＋玉Trail | `b34cbfa`（spec #93） |
| 可変結合ドリフト | 205 | 全域重力倍率が緩やかに呼吸。Quintom/dualH0/siphon/signIde排他。35%ロール | 鈍金四辺目盛り＋玉Field | `f0d2157`（spec #91） |
| 壊滅的photo-zゲート | 214 | OBB進入20%で進行方向±36pxシフト。strings/holo/pdg排他。35%ロール | 二重影欠け線＋玉Field | `9bbeaae`（spec #94） |
| 青傾き原始ハム | 217 | 深度でねじれ周波数上昇（青スペクトル）。hdHum/alens/gwb/echo/GW排他。35%ロール | 四隅点下ほど高速明滅＋玉Twist | `62c9787`（spec #95） |
| S8分岐縫い目 | 222 | 縫い目で重力成長振幅1.06/0.94＋横断ねじれ。dualH0/nuNull/signIde/varCoup排他。35%ロール | 錆藍二密度点線＋玉Trail/Field/Twist | `ab52513`（spec #96） |
| 等方宇宙複屈折ドリフト | 231 | 全域片手回り微ねじれβ=±0.0025。複屈折シート/alens/blueHum族排他。35%ロール | 四隅片手位相ずれ＋玉Twist | `9245e71`（spec #99） |
| バリオンフィードバック軟化域 | 225 | 楕円内連続ソフトニングv*=0.985。mBias/silk/ia排他。35%ロール | 銅灰吐き出しストリーク＋玉Trail | `fd6e47c`（spec #97） |
| カメレオンスクリーン場 | 228 | 近傍ペグ密度で力が減衰。ideSiphon/darkHalo/sidm排他。35%ロール | 苔灰欠け円弧＋玉Field | `4568c78`（spec #98） |
| Lyα BAOゴースト環 | 234 | 見た目と物理半径がずれたリングの法線弱引力。BAO/ORC/gravWaves排他。35%ロール | 錆白実リング＋ずれたゴースト点線＋玉Trail | `ded04f2`（spec #100） |
| フレックスノット重力波ハム | 237 | 深度閾値でω段差ジャンプする全域ねじれ。blueHum/hdHum/alens/gwb/echo/gravWaves/isoBire排他。35%ロール | 横節線＋四隅帯別明滅＋玉Twist/Field | `fca4a90`（spec #101） |
| マグネター誕生チャープ | 242 | パルス外向き＋chirpNで増すLense-Thirringねじれ。magnetars排他。35%ロール | 氷琥珀リング密化＋淡青スパイラル＋玉Force/Twist | `5903a63`（spec #102） |
| RMフレア回廊 | 245 | 斜め帯掃引で1横断1回±0.18radねじれ。FRB/CME排他。35%ロール | 電波緑帯＋端明滅＋玉Twist/Trail | `3b27d44`（spec #103） |
| 減光DM殻 | 248 | 膨張殻帯で微ドラッグ+弱外向屈折。ORC/gravWaves排他。35%ロール | 灰緑環＋触れ弧点灯＋玉Field | `e069f78`（spec #104） |
| 持続電波星雲 | 251 | 固定円内等方ドラッグ+0.05外向。silk排他。35%ロール | 電波緑ハロー＋遅い核＋玉Trail | `5206619`（spec #105） |
| GWB–LSS異方バイアス | 254 | 固定軸へ四重極加速。ハム/ねじれ族排他。35%ロール | 四隅軸マーカー＋玉Field | `e86e2dd`（spec #106） |
| 超軽量DMうねり | 257 | 全域微ねじれ（速度保存）。flexHum/ハム族/gwbLss排他。35%ロール | 背景塵の同相横揺れ＋玉Twist | `0c2ca90`（spec #107） |
| JWST星形成効率過剰域 | 262 | 楕円内v*=1.008+微外向。silk/prs排他。35%ロール | 骨白吐き出し+玉Trail | `5a74b58`（spec #108） |
| DE摂動クラスタリング幕 | 265 | OBB内重力0.90＋微ねじれ。Quintom/phantom/ide/signIde排他。35%ロール | 霜藍欠け膜＋玉Field/Twist | `0c4c79e`（spec #109） |
| 有効負ニュートリノ質量流 | 268 | 全球v*=1.0035。nuNull/dualH0排他。35%ロール | 四隅膨張点＋玉Trail | `b53a28b`（spec #110） |
| FRB旧種族遅延バースト | 271 | 予告120f・±0.16rad一斉ねじれ。frb/rm排他。35%ロール | 暗い発信源＋遅い弧＋玉Twist | `06a0c70`（spec #111） |
| 空間曲率キール | 274 | 縁帯で進行角を中心向き/外向きへ。bubble/ccc排他。35%ロール | 縁の欠け弧呼吸＋玉Twist | `6308490`（spec #112） |
| 相互作用HDE移譲帯 | 277 | 水平帯で片側軸力＋反対側ドラッグ。phantBelt/Quintom/bigRip/ide排他。35%ロール | 二色欠け横帯＋玉Field/Trail | `d557a13`（spec #113） |

---

## 6. 新ハザード配線の雛形（8タッチポイント）

新しいハザード/ギミックは、既存の**重力レンズ**（連続フォース型）または**彗星**（衝突型）をテンプレにコピーする。
すべて `src/components/CryptoPeggleGame.tsx` 内。

1. **interface 定義** — L76-90（`Comet`/`Lens`/`Wormhole` の近く）
2. **`GameState` フィールド追加** — L185-192
3. **`useRef` 初期 state** — L1351-1355（全フィールド初期化必須）
4. **`generateLevel` 生成** — L1157-1208。専用 `makeRng` ＋レベルゲート＋確率ロール。
   **戻り型(L878) と 最終 return(L1210) の両方も更新**すること。
5. **`initLevel` 代入** — L1429-1437（分割代入 → `g.<field> = <field>`）
6. **per-ball フォース**（連続力の場合） — 物理セクション L2822-2874（レンズの接線力 2843-2858 が最小例）
7. **サブステップ衝突**（跳ね返し/消滅の場合） — L2948-2999（高速ハザードのトンネル防止に必須）
8. **描画ループ** — 各ブロック末尾で必ず `ctx.globalAlpha = 1` に戻す

- 連続フォース型のみ → **重力レンズ**をコピー（衝突なし）。
- 跳ね返し/消滅型 → **彗星**もコピー（ステップ7を追加）。

> 行番号は目安。編集で少しずれることがあるので、実装時は近傍のシンボル名で位置を確認すること。

---

## 7. 物理エンジンの仕組みと新ギミック適応ガイド

> このセクションは **crypto-peggle 専用**。他プロジェクトには適用されない。
> 目的：新しい宇宙ギミックを追加するたびに「物理をどう繋ぐか」を毎回ゼロから考えなくて済むようにする。

### 7.1 物理はベクトル演算のみ（Box2D 等のリジッドボディエンジンは不使用）

このゲームに汎用物理エンジンは無い。すべて `src/components/CryptoPeggleGame.tsx` 内の
**手書きの毎フレーム加算 + 円/OBB 当たり判定**で完結している。基本単位：

```
ball.vy += 力の加速度        // 速度に直接足す（質量=1固定）
ball.x  += ball.vx / substeps // 位置は速度をサブステップで積分
```

真の物理シミュレーションではなく「ゲームフィールとして気持ちよく曲がる/跳ねる」ことが目標。
新ギミックの力の強さも、数式の正確さより**触った時の感触**で調整してよい。

### 7.2 1フレームの処理順序（L3078〜、`g.phase === 'firing'` ブロック内）

新しい力/衝突は必ずこの順序のどこに差し込むかを意識する。既存の並びを変えないこと。

0. freeze/mud タイマー減衰 + `effMinSpeed` 算出 — L3088-3094
1. stuck/rescue 判定（詰み救済） — L3096-3110
2. **重力** `ball.vy += effGrav`（速度ランプ込み） — L3112-3114
3. **連続フォース群**（範囲内なら velocity に加算、衝突なし）
   - ブラックホール半径引力 — L3116-3134
   - 重力レンズ接線力 — L3138-3152
   - 風（ゾーン限定） — L3155-3161
   - CME 掃引 — L3164-3168
   - 磁石ペグ引力 — L3171-3181
   - **新しい「常時作用する場」型ギミックはここに追加**（磁石/レンズと同じ書き方）
4. **サブステップ移動 + 高速衝突**（壁・バンパー・彗星・ワームホール） — L3183-3296
   - 各サブステップの中で「移動→即座に衝突判定」を繰り返す＝トンネリング防止の要
   - **新しい「接触で跳ね返る/消す/テレポートする」型ギミックはここに追加**
5. **ペグ衝突**（軽い円判定、サブステップ無し） — L3299〜
   - ペグは動かない・小さいので通常速度なら貫通しないため、サブステップ不要

### 7.3 「連続フォース型」と「接触衝突型」のどちらで実装するか

| 特徴 | 連続フォース型（レンズ/磁石/ブラックホール） | 接触衝突型（彗星/バンパー/壁） |
|---|---|---|
| 効果 | 範囲内にいる間ずっと `vx/vy` に加算 | 円/OBB が重なった瞬間だけ反応 |
| 実装場所 | §7.2 の 3. | §7.2 の 4.（サブステップ内） |
| 高速弾のすり抜け対策 | 不要（毎フレーム働くので抜けない） | **必須**（サブステップに置かないと高速弾が貫通する） |
| 典型パターン | `dx,dy` → 正規化 → 距離減衰 `t*t` で強さを絞る | 距離二乗比較 → 法線反射 `v -= 2*dot(v,n)*n` |

判断基準：「触れた瞬間だけ効くべきか」→ 衝突型。「近づくほどジワジワ効くべきか」→ フォース型。
霧やCMEのような「範囲に入っている間だけ効く」ものもフォース型に分類する（風・CMEを参照）。

### 7.4 速度まわりの不変条件（新ギミックが壊しやすいポイント）

- **`effMinSpeed` によるクランプ**（L3092-3094, 衝突後に個別に呼ばれる）— 全ての衝突ハンドラは
  反射/減速後に `if (spd < effMinSpeed) { rescale }` を呼んでいる。新しい衝突型ギミックも
  ボールを減速させるなら、この最小速度クランプ相当を入れる（例：mud/freeze ペグ参照 L3361-3383）。
  入れないと極端に遅いボールが stuck 判定に頻繁に引っかかる。
- **`BALL_SPEED * 2` によるハードクランプ**（風で使用 L3159, `computeTrajectory` 内 L1475）—
  無限に加速する力（風の累積等）は速度上限を明示的に切る。切らないとサブステップ数が
  際限なく増えて重くなる。
- **stuck-rescue（L3096-3110）とは競合しない**設計にする — 新ギミックが「ボールを長時間
  同じ場所に留める」場合（重い減速・引力）、220フレーム無進捗で強制救済が入ることを前提にしてよい
  （詰み厳禁 §4-4 はこの仕組みで担保されている）。ただし救済されるまでの間、見た目上
  「詰んでいるように見える」演出は避ける。
- **サブステップ数はその場の速度で決まる**（L3187 `Math.ceil(spd0 / BALL_R)`）— フォース型ギミックが
  1フレームで大きく速度を変える（例：ブラックホール直近）と、次フレームの substeps が急増して
  重くなることがある。強さの上限（`t*t` の距離減衰、力の定数）で自然に頭打ちにすること。

### 7.5 `computeTrajectory`（トラジェクトリ・プレビュー, L1465-1494）は絶対に触らない

- ここは**照準線専用の簡易シミュレータ**で、計算するのは重力・風・壁反射（warp/通常）・
  ペグ到達での停止だけ。ブラックホール引力・レンズ・彗星・ワームホール・CME・磁石・バンパー・
  壁セグメント（void/warp/distort）は**意図的に含まれていない**。
  磁石ペグはペグ停止判定からも除外されている（L1487 `p.type === 'magnet'` を skip）＝
  プレビュー線は磁石を素通りする。これも仕様であり直さないこと。
- §4-2「プレビュー無反映＝フェアな難しさ」の実装上の正体はこれ。新ギミックの力・衝突ロジックを
  ここに追加してしまうと「読める宇宙」になり North Star（§1）の設計思想に反する。
- 例外は無い。新ギミックがどれだけ強くても `computeTrajectory` は変更しないこと。

### 7.6 決定論（同一シードで同一レベル形状になる）の保ち方

- レベル生成は `generateLevel` 内で単一の `rng`（シード=レベル番号由来）を消費する。新ギミックの
  出現判定・座標・強さ決定に**メインの `rng` を直接使うと消費順がずれ、既存ギミックの配置も変わる**。
- 既存ハザードは `makeRng((rng()*0x100000000)>>>0)` で**メイン rng から1回だけ種を引いて
  専用ストリームを分岐**させている。実在する分岐は6本：`gimmickRng`(L1173) / `bumperRng`(L1215) /
  `whRng`(L1233) / `chainRng`(L1248) / `wallRng`(L1364) / `hazardRng`(L1404)。
  新ギミックも同じ作法で7本目を足す（§4-3 の実体）。
- 実行時の毎フレーム挙動（引力の適用・当たり判定）は決定論を要求されない
  （`Math.random()` を使ってよい。歪み壁の乱反射 L3216, stuck-rescue の横ジッター L3105,
  hash ペグのランダム方向 L3356, 彗星の出現速度 L2632, バーストの飛散方向などは非決定的。
  なお彗星の**反射自体**（L3257-3265）は決定的な法線反射＋運動量キャリー）。
  **決定論が必要なのは「レベル生成時」だけ**。

### 7.7 新ギミックの物理パラメータ設計チェックリスト

1. 力/衝突の種類を決める（§7.3 の表で分類）。
2. 強さ定数をファイル冒頭の `// ─── Constants` ブロックに追加（既存の同カテゴリ定数の近くに置く、
   例：引力なら `BH_PULL_FORCE`/`MAGNET_FORCE` の並び）。
3. 距離減衰は `t = 1 - dist/range; strength = BASE * t * t`（二乗減衰）が既存ハザード共通の書き方
   — 新ギミックもこれに揃えると「宇宙の場」としての手触りが統一される。
4. 衝突型なら反射後に **`effMinSpeed` クランプ**を必ず入れる（§7.4）。
5. `computeTrajectory` には**絶対に反映しない**（§7.5）。
6. レベル生成の乱数は**専用 `makeRng` で分岐**（§7.6）。
7. 詰み厳禁（§4-4）— 恒久的にボールを静止させる力にしない。stuck-rescue（220f）で必ず抜けられる
   強さに留める。
8. §6 の8タッチポイント（interface / GameState / useRef 初期化 / generateLevel / initLevel /
   フォース or サブステップ衝突 / 描画）を一通り確認する。
